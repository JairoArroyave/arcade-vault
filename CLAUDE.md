# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Project

Arcade Vault ("arcade.-vault") — a platform for playing games online and competing for the highest score. Currently a fresh `create-next-app` scaffold (app/page.tsx and app/layout.tsx are still the default template); no game, scoring, or data-persistence logic has been implemented yet.

## Commands

```bash
npm run dev      # start dev server (Next.js, Turbopack) at localhost:3000
npm run build    # production build
npm run start    # run the production build
npm run lint     # eslint (flat config, eslint.config.mjs)
```

There is no test runner configured in package.json yet — check with the user before assuming Jest/Vitest/Playwright conventions.

## Architecture

- Next.js 16 App Router, React 19, TypeScript (strict), Tailwind CSS v4 via `@tailwindcss/postcss`.
- `app/` is the App Router root: `app/layout.tsx` defines the root HTML shell and loads the Geist fonts; `app/page.tsx` is the `/` route.
- Path alias `@/*` maps to the repo root (`tsconfig.json`).
- ESLint uses the flat-config format (`eslint.config.mjs`), extending `eslint-config-next`'s `core-web-vitals` and `typescript` rule sets.
- **This project runs on a pre-release Next.js version with breaking changes from the Next.js you were trained on.** Before writing any Next.js-specific code (routing, data fetching, config, conventions), consult `node_modules/next/dist/docs/` for the current APIs — do not rely on prior training knowledge of Next.js conventions. See `AGENTS.md` (imported above) for details; that file is auto-regenerated by `next dev` and should be committed as-is when it changes.

## Workflow

This project follows Spec Driven Design using the `/spec` and `/spec-impl` workflow from [Klerith/fernando-skills](https://github.com/Klerith/fernando-skills), installed via:

```bash
npx skills@latest add Klerith/fernando-skills
```

If these skills/commands aren't present yet in `.claude/`, ask the user before improvising a different workflow.


## Skills 
usa siempre /frontend-design para diseñar la interfaz de usuario
